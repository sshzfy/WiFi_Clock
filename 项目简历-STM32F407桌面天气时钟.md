# 项目简历 · STM32F407 桌面天气时钟

> 面向求职的嵌入式软件项目经历材料。
> **本文档基于 2026-09-18 的代码快照逐文件核对**（含按键交互任务、ESP32-C3 Wi-Fi 省电档位、资源目录重构等最新改动）。
> 用法：第 0 节用于在线简历表单/邮件正文；第 1 节粘贴进简历正文；第 2–6 节用于项目介绍与面试讲述；第 7 节按目标岗位选一套；第 8 节是投递前检查清单。

---

## 0. 一页纸速览版（纯文本，可直接粘贴到在线表单）

```text
STM32F407 桌面天气时钟 / 室内环境监测终端
角色：嵌入式软件独立开发        平台：STM32F407VET6 + FreeRTOS

- 独立完成一款桌面天气时钟终端：ESP32-C3 AT 模组联网，SNTP 校时 + 天气 API 获取实况天气，
  ST7789 彩屏（240x320）显示时钟/日期/天气/室内温湿度，SSD1306 OLED 负责夜间显示，
  光敏电阻自动切换昼夜模式并支持按键手动干预，夜间进入低功耗状态。
- 设计 FreeRTOS 五任务架构（UI/网络/传感器/光敏/按键）+ 9 事件位，以事件组与任务通知替代轮询，
  落实"LCD 单写者、USART1 单占用、共享数据临界区整块替换"三条并发约定，消除显示撕裂与 AT 串扰。
- 将 839KB 字库与图片从 MCU 内部 Flash 迁移到 8MB W25Q64 + littlefs：程序总 ROM 由 347KB
  降至 54.9KB（-84.2%）；字模改为 O(1) 区位码定位（替代平均 1878 次字符串比较），
  并用 DMA 乒乓双缓冲把 Flash 读取时间完全隐藏在屏幕发送之后。
- 实现夜间低功耗：冻结网络/传感器周期任务并按独立事件位立即补更，切换 ESP32-C3 到
  Modem-sleep 档位（约 80mA → 20mA，Wi-Fi 连接保持不掉线）；夜间时间源切到 DS1302 外部 RTC
  （burst 读 + CH 停振位检测 + 写后回读校验 + 失败回退软件时钟），断网仍可正常走时。
- 实现按键手势识别（单击/双击/三击/长按，800ms 长按阈值 / 300ms 连击窗口），按键请求交由
  光敏任务统一裁决并记录自动判定基线，避免两处状态机互相打架。
- 交付三种构建模式（RTOS 运行/资源烧录/裸机模块测试）与 W25Q64、littlefs 共 20 项自测用例；
  资源缺失时渲染安全降级为纯色，不阻塞任务、不重启、不自动格式化。

技术栈：STM32F407 | Cortex-M4 | FreeRTOS | SPI+DMA | W25Q64 | littlefs | ST7789 | SSD1306
        DHT22 单总线 | DS1302 | AT 指令(含 AT+SLEEP) | SNTP | JSON 解析 | EXTI/ADC | Keil MDK | ST-Link
代码规模：自研 C 代码约 7300 行    Flash 54.9KB/512KB    SRAM 124.2KB/128KB    5 任务
```

---

## 1. 简历正文版（可直接粘贴，约 450 字）

**STM32F407 桌面天气时钟 / 室内环境监测终端** ｜ 嵌入式软件独立开发 ｜ STM32F407VET6 + FreeRTOS

- 独立完成一款桌面天气时钟终端：ESP32-C3 AT 模组联网，SNTP 校时 + 心知天气 API 获取实况天气，ST7789 彩屏（240×320）显示时钟/日期/天气/室内温湿度，SSD1306 OLED 负责夜间显示，光敏电阻自动切换昼夜模式并支持按键手动干预，夜间进入低功耗状态。
- 基于 FreeRTOS 设计**五任务架构**（UI / 网络 / 传感器 / 光敏 / 按键，5 级优先级）+ 9 个事件位，以事件组与任务通知替代轮询，贯彻"LCD 单写者""USART1 单占用""共享数据临界区整块替换"三条并发约定，消除显示撕裂与 AT 响应串扰。
- 将 839KB 字库与图片从 MCU 内部 Flash 迁移到 8MB W25Q64 + littlefs：MCU 内不再保留任何点阵/像素数据，**程序总 ROM 由 347KB 降至 54.9KB（−84.2%）**；自研字模 O(1) 区位码定位替代线性查表（平均 1878 次字符串比较），并用 DMA 乒乓双缓冲把 Flash 读取时间完全隐藏在屏幕发送之后。
- 设计"夜间低功耗 + 双屏切换"：网络与传感器周期任务冻结、退出时按独立事件位立即补更，配合光敏中断 2s 去抖与 ACK 握手避免竞态；把 ESP32-C3 切到 **Modem-sleep**（约 80mA → 20mA，Wi-Fi 连接保持），夜间时间源切到 DS1302 外部 RTC（burst 读 + CH 停振位检测 + 写后回读校验 + 失败回退软件时钟）。
- 实现按键手势识别（单击/双击/三击/长按；800ms 长按阈值、300ms 连击窗口），按键请求统一交由光敏任务裁决并记录"自动判定基线"，避免两处状态机互相覆盖；修复 TIM5 微秒计时在计数器回绕瞬间的时间倒流问题，保证 `delay_us` 单调可靠。

**技术关键词**：STM32F407 · Cortex-M4 · FreeRTOS · 事件组/任务通知 · SPI + DMA · W25Q64 · littlefs · ST7789 · SSD1306 · 单总线 DHT22 · DS1302 · AT 指令 · SNTP · JSON 解析 · EXTI/ADC · 低功耗设计 · Keil MDK · ST-Link

---

## 2. 项目基本信息

| 项目 | 内容 |
| --- | --- |
| 项目名称 | STM32F407 桌面天气时钟 / 室内环境监测终端 |
| 项目角色 | 嵌入式软件独立开发（驱动 → 应用 → 资源存储方案全流程） |
| 主控平台 | STM32F407VET6（Cortex-M4F，168MHz，512KB Flash，128KB SRAM + 64KB CCM） |
| 软件平台 | FreeRTOS（heap_4，88KB 堆，tick 1000Hz，事件组/任务通知/互斥量）+ littlefs v2.11 + STM32F4 标准外设库 |
| 开发环境 | Keil MDK-ARM Plus 5.24.1 / ARMCC V5.06（AC5）/ C99 / ST-Link |
| 代码规模 | 自研 C 代码约 7300 行：BSP 驱动 26 个文件（约 3600 行）、应用层 19 个文件（约 2970 行）、资源描述层 229 行、littlefs 适配 475 行；Keil 工程 9 个分组 |
| 成果指标 | 程序总 ROM 54.9KB（Flash 占用 10.7%）、SRAM 124.2KB、839KB 资源外置存储并由 littlefs 管理 |

### 硬件构成

| 模块 | 型号 / 参数 | 作用 |
| --- | --- | --- |
| 彩屏 | ST7789V，SPI3 @21MHz，240×320，RGB565，DMA1_Stream5 | 主界面显示 |
| 夜间屏 | SSD1306 0.96"，128×64，软件 I2C（PB6/PB7） | 夜间时间显示 |
| 联网模组 | ESP32-C3-MINI-1，AT 固件 v4.1.1.0，USART1 @115200 | WiFi / SNTP / HTTP / 省电档位 |
| 温湿度 | DHT22 / AM2302 单总线（PE6） | 室内环境采集 |
| 光照 | 光敏电阻模块，DO(EXTI1) 或 AO(ADC + 模拟看门狗) 二选一 | 昼夜自动切换 |
| 按键 | 独立按键 PA0 + EXTI0（空闲下拉为低，按下为高，硬件消抖） | 手动切换昼夜，双击/三击/长按预留 |
| 外部 Flash | W25Q64，8MB，SPI1 @21MHz | littlefs 存放全部字库与图片 |
| 外部 RTC | DS1302（PE7/PE8/PE9） | 夜间低功耗时间源 |
| 调试口 | USART2 @115200，DMA 发送 | 日志与调试 |

### 工程目录（资源已独立为顶层目录）

```
User/            应用层：main.c（三种模式入口）/ Board.c / App.c / app_task.c /
                 Provision.c / bare_test.c / Boot_Page.c / Main_Page.c + Inc/
BSP/             板级驱动 13 类（26 个文件）：Asset / LCD / OLED / AT / I2C / DHT22 /
                 External_RTC / Light_Sensor / Key / Timer / Usart / W25Q64 / Profiling
Resource/        资源描述与数据：Inc(Font.h/Image.h) + Src/Font(字库) + Src/Image(图片)
Third_Lib/       FreeRTOS（内核 + portable）+ LittleFS（含 lfs_port 与读写封装）
Core/ + STM32F4xx_StdPeriph_Driver/   CMSIS、启动文件、标准外设库
MDK/             Keil 工程（9 个分组）+ 构建产物
Documents/       器件手册与项目文档
```

### 自研范围与第三方依赖（面试可能追问，建议如实掌握）

| 类别 | 内容 |
| --- | --- |
| **自研** | 13 类 BSP 驱动：W25Q64（全部指令/页编程/跨页写/擦除/忙等待超时）、ST7789（渲染 + DMA 乒乓缓冲）、SSD1306、DHT22 单总线（us 级时序 + 6 种错误码 + 失败重试）、DS1302（burst 读 + CH 检测）、Key（EXTI0 双沿 + 手势上报）、软件 I2C、光敏（EXTI / ADC + 模拟看门狗）、TIM5 时基（ms/us 计时与 delay）、USART（环形缓冲 + printf-DMA）、DWT 计时、Asset 资源层；应用层 6 个模块 + 开机页与主界面绘制逻辑；littlefs 的 read/prog/erase 移植回调与读写封装（475 行） |
| **第三方** | STM32F4xx 标准外设库、FreeRTOS 内核（tasks/queue/event_groups/heap_4/port）、littlefs v2.11 本体、CMSIS 内核头与启动文件 |
| **资源数据** | 字模与图片由 PCtoLCD2002 按指定取模设置（横向取模、行优先、低位对应最左像素）生成，不属于手写代码 |

---

## 3. 详细版项目经历（面试 / 项目介绍用）

### 3.1 项目背景与目标

做一个放在桌面上长期无人值守运行的天气时钟：白天用彩屏展示时钟、日期星期、WiFi 状态、实况天气与室内温湿度；夜间自动切到低亮度 OLED 只显示时间，并降低系统负载与功耗；同时支持按键手动干预昼夜模式。核心难点不在单个外设能否点亮，而在于**多任务并发下的状态一致性、断网/掉线时的自恢复、传感器与按键两个输入源对同一状态的仲裁、以及资源受限下 839KB 字库图片的存放**。

### 3.2 系统架构

```
应用层     main.c（RTOS/资源烧录/裸机测试三模式）/ Board.c / App.c / app_task.c /
           Provision.c / bare_test.c / Boot_Page.c / Main_Page.c
板级驱动   Asset / LCD / OLED / AT / I2C / DHT22 / External_RTC / Light_Sensor / Key /
           Timer / Usart / W25Q64 / Profiling
操作系统   FreeRTOS（heap_4 / 事件组 / 任务通知 / 临界区 / 互斥量）
文件系统   littlefs v2.11（lfs_port.c 对接 W25Q64，LFS_Operation 封装读写）
硬件抽象   STM32F4xx 标准外设库 + CMSIS
```

**5 个任务与优先级设计**

| 任务 | 优先级 | 栈 | 职责 | 周期 |
| --- | --- | --- | --- | --- |
| `UI_Task` | 3 | 4KB | 创建事件组、板级初始化、开机页→结果页→主界面；LCD 唯一写者；昼夜切换执行者 | 白天 500ms / 夜间 2s |
| `DHT22_Task` | 4 | 2KB | 单总线温湿度采集，失败自动重试一次 | 10s |
| `Net_Task` | 2 | 4KB | 独占 USART1/AT：联网、SNTP、天气、Wi-Fi 省电档位，1s 心跳推进三个周期计数器 | 1s 心跳 |
| `LightSensor_Task` | 2 | 2KB | 光敏中断处理、去抖、昼夜状态唯一裁决者（含按键请求） | 事件驱动 + 1s 兜底 |
| `Key_Task` | 2 | 4KB | 按键边沿识别与手势判定（单击/双击/三击/长按），手势分发 | 事件驱动 |

优先级不是随手定的：**DHT22 优先级高于 UI**，是因为单总线协议对 us 级时序敏感，被 UI 抢占会直接读到校验和错误；**net/sensor/key 都低于 ui**，避免一次长 AT 事务（最长 10s）把显示饿死。5 个任务栈合计约 16KB，全部从 88KB 的 FreeRTOS 堆中分配。

**并发约定（三条硬规则）**

1. **LCD 只由 UI_Task 写**：net/sensor/light/key 任务只更新数据并置事件位，杜绝多任务同时操作 SPI3。
2. **USART1 只由 Net_Task 使用**：AT 响应是"请求—等待"模式，第二个使用者必然造成串扰。
3. **共享数据整块替换**：`wifi_info` / `weather_info` / `room_info` / 软件时钟的 `epoch + sync_ms` 全部在 `taskENTER_CRITICAL()` 内整体赋值（WiFi 信息还封装为 `WiFi_Info_Store()` 统一入口），UI 永远读不到半写状态。

**事件驱动的数据流**：9 个事件位区分为"数据就绪"（`EV_WEATHER` / `EV_DHT22` / `EV_WIFI` 变化）与"动作请求"（`EV_LOWERPOWER` 进夜间 / `EV_WAKEUP` 回白天 / `EV_NET_UPDATE_NOW`、`EV_DHT22_UPDATE_NOW` 立即补更），UI 用一次 `xEventGroupWaitBits` 同时等待多类事件，避免任何任务轮询对方状态。

### 3.3 核心工作与技术难点

#### 难点一：839KB 字库与图片放不进 512KB Flash —— 迁移到 W25Q64 + littlefs

- **问题**：11 张字模表（GB2312 一级汉字 3755 字 × 16/22 点阵 + 9 张 ASCII 表）与 32 张图片（2 张全屏图 + 30 张图标）合计约 839KB，编译进固件时 RO-data 达 323604 字节，总 ROM 347KB，Flash 余量紧张且无法再扩展界面。
- **方案**：在 W25Q64 上跑 littlefs（覆盖 8MB / 2048 个 4KB 块），把全部点阵与像素数据以文件形式存放，MCU 内只保留 `Font_t` / `Image_t` 描述表；新增 `Asset` 资源访问层统一负责挂载、自检与读取。
- **优化点**：
  - **字模 O(1) 定位**：按 GB2312 区位码直接算偏移 `((区码-0xB0)×94 + (位码-0xA1)) × 每字字节数`，替代旧实现的线性查表（每个汉字平均要 `strcmp` 1878 次）。
  - **常开文件句柄 + 位置缓存**：11 个字库文件句柄常驻，记录当前偏移，位置相同则跳过 `lfs_file_seek`，避免 CTZ skip-list 反复遍历。
  - **图片乒乓双缓冲**：分配 2×7680 字节缓冲，一块交给 DMA1_Stream5 发送时 CPU 同时从 SPI1 读下一块。**SPI1 与 SPI3 同为 21MHz** 是这一优化成立的前提——读 Flash 的时间被完全隐藏在屏幕发送之后；分配失败自动退化为单缓冲，不影响功能。
  - **统一图片清单**：`ImageTable.c` 提供 `g_AllImages[]` / `g_AllImageCount`，开机自检与烧录遍历共用同一份清单，避免两处硬编码不同步。
- **结果**：RO-data 323604 → **3748 字节**，总 ROM 355556 → **56184 字节（−84.2%）**，Flash 占用降到 54.9KB/512KB。
- **工程化取舍**：资源总量超过 512KB Flash，单次编译装不下，因此设计了**三批编译烧录方案**（批次 1 汉字库 / 批次 2 全屏图 / 批次 3 ASCII 表 + 图标），用 `RESOURCE_PROVISION` + `PROVISION_BATCH` 两个宏控制，并在 `main()` 里提供独立的烧录模式（不启动 RTOS、不初始化 LCD，只开 W25Q64 与串口）。

#### 难点二：夜间低功耗、双屏切换与"两个输入源争一个状态"

- **方案**：光敏任务检测到亮度跳变后先做 2s 去抖（期间新中断重新计时），再置事件位请求切换，并**等待 UI 任务回 ACK（超时 2s）**；UI 任务进入夜间时先"用网络时间回写 DS1302 并回读校验"→ 置低功耗标志 → 开 OLED 显示时间 → 关 ST7789 显示与背光。
- **关键细节**：夜间 `Net_Task` / `DHT22_Task` **冻结周期计数器且不执行任何 update**（`if (s_lowpower) continue;` 必须放在 `--counter` 之前，否则退出夜间时三个计数器同时归零、同一秒内连发多次 AT 事务）；退出夜间时 UI 通过 `EV_NET_UPDATE_NOW` / `EV_DHT22_UPDATE_NOW` 两个**独立事件位**通知两个任务立即补更。
- **踩坑记录（事件组清除式等待）**：最初两个任务共用同一个事件位，先被唤醒的任务会把位清掉，另一个永远收不到通知，表现为"退出夜间后只有一半数据被刷新"；拆成两个位后解决。
- **模组侧省电（新增）**：夜间把 ESP32-C3 切到 **Modem-sleep 档位（`AT+SLEEP=1`）**，RF 按 AP 的 DTIM 周期关闭，电流约从 80mA 降到 20mA；**只改变 RF 开关占空比，Wi-Fi 连接保持，因此退出夜间无需重新连接**。档位同步必须在补更之前执行（先恢复全速再发 WiFi/SNTP/天气），设置失败则保持原状态、下周期自动重试。更激进的 Light-sleep（约 130µA）因需先 `AT+SLEEPWKCFG` 配好唤醒源、而当前硬件未连唤醒 GPIO，评估后未采用——这是有依据的取舍而非能力缺失。
- **按键与光敏的仲裁（新增）**：按键单击也要切昼夜，但昼夜状态机在光敏任务手里。做法是按键任务**只置标志 + `xTaskNotifyGive` 唤醒光敏任务**，由光敏任务统一裁决，并记录 `s_baseline_dark`（当前环境光基线），使手动切换后光敏不会立刻改回去——只有环境光真正变化后自动跟随才重新生效；去抖期间若来按键则立即结束去抖优先响应。
- **夜间时间源切换**：`DS1302_SetTime()` 恒返回成功（无写校验），因此改为**写后回读比对"当日分钟总数"**，既容忍写耗时造成的 2 分钟偏差又能跨小时；同时检测秒寄存器 **CH（Clock Halt）位**，CH=1 说明振荡器停走，直接判定失败并回退到软件时钟——否则界面会显示一个不动的钟而日志仍报 `time_source=RTC`。

#### 难点三：联网链路的健壮性

- **AT 指令层**：USART1 用 **RXNE 中断 + 512 字节环形缓冲**收数，AT 层按毫秒级超时轮询取出（响应缓冲 1024 字节），等待期间 `vTaskDelay(1)` 让出 CPU；封装 `AT_Init / AT+RESTORE / CWJAP / CWSTATE / CIPSNTPCFG / CIPSNTPTIME / HTTPCLIENT / SLEEP` 完整时序，并区分 OK / ERROR / BUSY / READY 四种应答。
- **"查询失败 ≠ 掉线"**：`Service_WiFi_Update()` 先重试 2 次 AT 状态查询，两次都失败只上报失败、**不触发 CWJAP 重连**，避免模组繁忙/串口残留造成的误判引发无谓重连；只有确认未连接才重连，重连后轮询 3 次确认新状态，失败则保守置为离线等下一周期纠正。
- **天气 JSON 解析**：不引入 JSON 库，直接在响应体中按字段定位 `results → location → name/path` 与 `now → text/code/temperature`；`Main_Page.c` 中 26 条天气码映射表（复用 22 张天气图标）给出图标 + 中文描述，未知码回落为晴天。
- **层级自恢复**：WiFi 掉线自动重连，重连成功后立即补一次 SNTP 与天气；SNTP 失败按 5s、天气失败按 60s 重试，成功后分别回到 4h / 1h 周期；`AT_SNTP_Get_Time` 会把 1970 年之类的无效时间判为失败继续轮询。
- **软件时钟**：SNTP 校时后用 TIM5 1ms 计数推算当前时间，日期换算采用**民用历算法**（Howard Hinnant），未同步时界面显示 `--:--` 而不是错误时间。

#### 难点四：时序驱动与"隐性状态"问题排查

| 现象 | 根因 | 处理 |
| --- | --- | --- |
| DS1302 出现随机位错误 | `delay_us` 用无符号差比较，微秒计时偶发回退时相减**下溢**成巨大值，循环立即退出，实测把 SCLK 半周期压到 200ns 以下 | 改**有符号差**比较：回退被当作"负的已过时间"继续等待 |
| 偶发"时间倒流"、延时抖动 | TIM5 的 CNT 已回绕但更新中断尚未执行，读到的 ms 仍是旧值 | `TIM5_Get_us()` 采用无锁一致性读：ms 变化则重读、CNT 较小时复检进位，并用 **UIF 标志**补齐未处理的那 1ms，保证单调不减 |
| 上电先闪出上次的旧画面 | ST7789 复位**不清 GRAM** | `ST7789_Init()` 在开显示与背光**之前**先刷黑 GRAM |
| 切回 RTOS 固件后 OLED 一直挂着裸机测试画面 | SSD1306 不断电就保持 GDDRAM 与显示状态 | 上电即 `OLED_Init()` + `OLED_Display_Off()`；`OLED_Init()` 内**先清屏、最后才 `0xAF` 开显示** |
| OLED 清屏耗时约 240ms | 逐列发送需要 1024 次单字节 I2C 事务 | 改为**按页整块写**，每页 1 次事务写 128 字节，共 8 次 |
| 遮挡光敏后毫无反应、串口也无日志 | 改了 GPIO 端口/引脚，但 EXTI 的 `PortSource`/`PinSource` 仍是旧端口，中断永不触发 | 两者必须成对修改（实测踩过一次 PC1 vs PA1） |
| 周一时间同步失败 | 星期语义混用：DS1302 寄存器为 1~7，旧代码写 `weekday-1` 得到 0 | 统一为 1~7，读回时 `week < 1` 直接判失败 |
| W25Q64 未焊接时程序卡死 | 忙等待无上限 | 加入 `W25Q64_BUSY_TIMEOUT` 超时计数与 `W25Q64_GetBusyTimeoutCount()` 统计 |
| DHT22 偶发读取失败 | 单总线易受瞬时干扰 | 单次尝试与重试分离：失败后延时 50ms 重试一次，仍失败才上报错误码 |

- **中断与 RTOS 约束**：NVIC 分组为 `PriorityGroup_4`；`configMAX_SYSCALL_INTERRUPT_PRIORITY = 5<<4`，因此 **TIM5（优先级 2，1ms 基准）中断里只做计数，禁止调用任何 FreeRTOS API**；USART1 / EXTI0（按键）/ EXTI1（光敏）/ ADC（光敏 AWD）优先级均为 5，允许调用 `...FromISR` 并在退出时 `portYIELD_FROM_ISR`。光敏与按键的 ISR 都只做 `vTaskNotifyGiveFromISR`，去抖与手势识别全部在任务上下文完成。
- **调试日志**：`printf` 重定向到 `fputc`，按行加锁（互斥量 + 行属主），遇 `\n` 或缓冲满时以 DMA 整行发出，保证多任务日志不交叉；调度器未运行或中断上下文中退化为逐字节轮询，使启动期与 HardFault 现场的日志也能打印（`vAssertCalled`、`vApplicationStackOverflowHook` 都依赖它）。
- **性能测量**：用 `DWT->CYCCNT` 实现微秒级计时（`Prof_Us()`），不占用定时器、不产生中断。

### 3.4 工程化与可维护性

- **三种构建模式由宏切换**，一份代码同时承载运行、生产与测试：
  | 宏组合 | 模式 | 行为 |
  | --- | --- | --- |
  | `RESOURCE_PROVISION=1` | 资源烧录 | 只初始化 W25Q64 与串口，不启动 RTOS、不初始化 LCD |
  | `USE_FREERTOS=1` | RTOS 运行 | 正式固件 |
  | `USE_FREERTOS=0` | 裸机模块测试 | 用 `BM_TEST_MODULE` 选择 OLED / 光敏 / DS1302 / W25Q64 / littlefs |
- **自测用例覆盖底层风险**：W25Q64 用例 T1~T9（JEDEC ID、页编程、跨页写入、扇区擦除、忙等待超时统计，测试区固定在末 4 个扇区不碰文件系统）；littlefs 用例 T1~T11（读写、目录、追加、64KB 大文件、遍历、删除、卸载重挂载持久化、`SaveFont`、`SaveImage`+`LoadImage`、缺失文件返回 `NOENT`）。
- **失败安全降级**：`Asset_Init()` 开机逐项核对文件是否存在、大小是否正确（字库按 11 项清单、图片按 `g_AllImages[]` 遍历），统计"已检查项 / 缺失项"；资源缺失时渲染退化为纯色背景并在串口告警，**绝不自动格式化文件系统**，不阻塞任务、不重启。
- **工程组织**：资源与代码分离为顶层 `Resource/`（描述头 + 字库 + 图片），界面逻辑归入 `User/Src/`；Keil 工程 9 个分组；引脚/周期/阈值/手势参数/编译开关全部宏定义集中管理，避免散落魔数；界面中文按 GB2312 双字节写入并配套区位码定位，保证字模不错位。
- **工程整洁**：`.gitignore` 忽略构建输出与 Keil 中间文件、个人窗口布局，保留工程文件与调试配置；提供 `KeilClear.bat` 一键递归清理中间产物。
- 附带项目文档 `Documents/README.md`（约 950 行）：目录结构、任务与事件位表、引脚分配、中断优先级约束、关键流程时序、资源布局与烧录步骤、已知限制清单。

### 3.5 项目演进四阶段（体现工程推进方式）

项目不是一次写完的，而是按"每步可编译、可烧录、可验证"分阶段推进，这份节奏本身就是可讲的工作方法：

| 阶段 | 内容 | 为什么这样做 |
| --- | --- | --- |
| **一、裸机分模块验证** | 用 `USE_FREERTOS=0` + `BM_TEST_MODULE` 开关，在无 RTOS 环境下依次跑通 OLED → 光敏 → DS1302 → W25Q64 → littlefs，每个测试自带死循环、可独立观测 | 底层驱动不通时上层必然失败；先跑通 W25Q64 才能把"硬件/SPI/时序问题"与"文件系统问题"分开定位 |
| **二、RTOS 集成** | 把已验证的驱动接入多任务 + 事件组架构，逐步补上临界区保护与优先级调整（DHT22 提到 4 保护 us 级时序） | 驱动已被证明可用，此时出问题必然出在并发设计上，排查范围收窄 |
| **三、资源外置与低功耗** | 引入 littlefs 资源层 + Provision 三批烧录 + DMA 乒乓缓冲；再叠加光敏昼夜切换、DS1302 夜间时间源、低功耗冻结策略 | 功能稳定后再做"省 Flash / 省负载"的优化，避免同时引入多个变量 |
| **四、交互与功耗细化** | 新增按键手势任务与手动/自动状态仲裁；把省电推进到模组侧（`AT+SLEEP` Modem-sleep 档位）；修复 TIM5 微秒计时的回绕一致性缺陷 | 从"能用"到"好用且省电"：交互引入新的状态竞争，正好用这套架构把它收敛到单一裁决者 |

宏开关贯穿始终：即使演进到第四阶段，裸机测试模式与资源烧录模式仍能一键构建，回归验证随时可做。

---

## 4. 量化成果一览

| 指标 | 数值 | 说明 |
| --- | --- | --- |
| 程序总 ROM | **355556 → 56184 字节（−84.2%）** | 资源外置后的 Flash 占用 |
| RO-data | **323604 → 3748 字节** | MCU 内只剩描述表与指针表（Code 52324 / RW 412 / ZI 126740） |
| Flash 占用 | 54.9KB / 512KB（约 10.7%） | 余量充足 |
| SRAM 占用 | 124.2KB / 128KB（FreeRTOS 堆 88KB + 渲染缓冲 22.5KB） | 静态余量约 3.9KB；堆内 5 个任务栈约 16KB + 图片乒乓缓冲 15KB，余量约 57KB |
| 外置资源规模 | 858778 字节（≈839KB）：2 汉字库 + 9 张 ASCII 表 + 32 张图片 | littlefs 实际占用约 247/2048 块 |
| 字模定位复杂度 | 线性查表（平均 1878 次 `strcmp`）→ **O(1)** | 区位码直接算偏移 |
| 图片传输 | 15KB 乒乓双缓冲，Flash 读取时间完全隐藏在 DMA 发送之后 | 全屏图约 58ms，受 SPI3@21MHz 发送带宽限制 |
| 并发规模 | **5 任务 / 5 优先级 / 9 个事件位 / 1 个互斥量** | 事件组 + 任务通知驱动，无轮询等待 |
| 交互能力 | 单击/双击/三击/长按 4 种手势（800ms 长按阈值 / 300ms 连击窗口） | 单击已接入昼夜切换，其余为扩展点 |
| 省电效果 | 夜间模组约 **80mA → 20mA**（Modem-sleep），Wi-Fi 连接保持 | 配合关屏与冻结周期任务 |
| 底层自测 | W25Q64 9 项 + littlefs 11 项 | 裸机模式可独立验证 |
| 自研代码 | 约 7300 行（BSP 26 文件 3613 + 应用层 19 文件 2965 + 资源描述 229 + 文件系统适配 475） | 不含原厂库与资源数据 |

---

## 5. 技能映射（对照岗位 JD 使用）

| 岗位要求常见表述 | 本项目可对应的证据 |
| --- | --- |
| 熟悉 ARM Cortex-M 平台开发 | STM32F407（Cortex-M4F）168MHz，PLL 时钟树、NVIC 优先级分组、DMA、EXTI、ADC、SPI、USART、TIM 全流程使用 |
| 熟悉 RTOS 原理与应用 | FreeRTOS 5 任务架构、优先级权衡、事件组、任务通知（`vTaskNotifyGiveFromISR` / `ulTaskNotifyTake`）、临界区、互斥量、heap_4、栈溢出与断言钩子、内核裁剪配置 |
| 熟悉常用外设驱动开发 | 独立编写 SPI Flash（W25Q64 全部指令/页编程/擦除/超时）、ST7789、SSD1306、DHT22 单总线、DS1302 三线 RTC、独立按键 EXTI、软件 I2C、光敏 ADC + 模拟看门狗 |
| 熟悉通信协议与联网 | ESP32-C3 AT 指令集（含 `AT+SLEEP` 省电档位）、WiFi 状态解析、SNTP 校时、HTTP 客户端、JSON 字段解析、断线重连与重试策略 |
| 熟悉文件系统 / 存储 | littlefs 移植（read/prog/erase 回调对接 SPI Flash）、挂载/自检/大文件读写/掉电持久化验证 |
| 低功耗设计 | 关屏 + 冻结周期任务 + 模组 Modem-sleep 档位；对 Light-sleep 的唤醒源约束做出有依据的取舍 |
| 具备调试与问题定位能力 | 定位"微秒计时回绕导致时间倒流""事件位被抢占""EXTI 端口源不匹配""DS1302 CH 停振""复位不清 GRAM"等 9 类隐性故障 |
| 代码规范与文档能力 | 分层架构、宏集中配置、失败安全降级、约 950 行项目文档与三模式构建说明 |
| 工具链 | Keil MDK-ARM、ARMCC V5.06、ST-Link 调试、DWT 性能剖析、Git 版本控制（含 `.gitignore` 规范） |

---

## 6. 面试可展开的四个技术故事（建议按 STAR 讲）

1. **"把 839KB 资源搬出 MCU"**：从"Flash 装不下"的问题出发 → 调研 littlefs + 外部 Flash → 自研资源访问层 → 用区位码把字模查找做成 O(1) → 用乒乓双缓冲把 Flash 读取藏进 DMA 发送 → 量化出 ROM 降低 84.2%。可延伸讨论：为什么不做 XIP（外部 SPI 无地址映射）、littlefs 掉电安全与磨损均衡、为什么保留源码内资源作为烧录备份。
2. **"延时函数偶发把 DS1302 时序压到 200ns"**：现象是随机位错误 → 排查到 `delay_us` 用无符号差比较，而微秒计时在计数器回绕瞬间会回退，相减下溢导致循环提前退出 → 改有符号差比较 → 顺带把 `TIM5_Get_us()` 做成利用 UIF 标志补齐回绕的一致性读，保证单调不减。可延伸讨论跨域读取硬件计数器的经典问题（无锁重读、进位检测、单调性保证）。
3. **"夜间只有一半数据被刷新"**：症状 → 排查到事件组清除式等待被两个任务共用 → 拆成独立事件位 → 补充"冻结计数器必须放在递减之前"的第二个坑。体现对 RTOS 原语语义的理解而非停留在 API 调用。
4. **"按键和光敏抢同一个状态"**：加按键时面对"两个输入源要控制同一个昼夜模式" → 决定让光敏任务成为唯一裁决者，按键只发通知 + 标志位 → 追加"自动判定基线"使手动切换不会被光敏立刻覆盖，仅当环境光真正变化才恢复自动跟随。体现状态机归属与竞态收敛的设计意识。

---

## 7. 岗位定制精简版（按目标岗位选一套粘贴）

### 7.1 投「MCU / 底层驱动工程师」

> **STM32F407 桌面天气时钟** ｜ 嵌入式软件独立开发
> - 独立编写 13 类外设驱动（BSP 层 26 个文件约 3600 行）：SPI Flash（W25Q64，页编程/跨页写/扇区擦除/忙等待超时策略）、ST7789 彩屏（SPI3 + DMA1_Stream5，命令 8 位/像素 16 位切换前等 BSY 并关 SPE）、SSD1306（软 I2C，按页整块写把清屏从 240ms 降到 8 次事务）、DHT22 单总线（us 级时序 + 校验和 + 6 种错误码 + 失败重试）、DS1302（burst 读 + CH 停振检测）、独立按键（EXTI0 双沿）、光敏（EXTI / ADC + 模拟看门狗含中断风暴防护）。
> - 实现微秒级时基的一致性读取：`TIM5_Get_us()` 无锁重读 + 进位复检 + UIF 补齐回绕毫秒，保证单调不减；`delay_us` 使用有符号差比较，修复无符号下溢导致的时序被压缩问题。
> - 用 DMA 乒乓双缓冲（2×7680B）把 8MB Flash 的图片读取完全隐藏在屏幕发送之后；用区位码把字模查找从线性查表优化为 **O(1)**。
> - 严格处理中断与 RTOS 边界：TIM5（优先级 2）只做计数不调用任何 FreeRTOS API，USART1/EXTI0/EXTI1/ADC（优先级 5）才允许 `...FromISR` + `portYIELD_FROM_ISR`。
> - 建立裸机自测用例 W25Q64 9 项、littlefs 11 项，驱动异常时以超时/错误码上报而非死等。

### 7.2 投「物联网 / 嵌入式应用工程师」

> **STM32F407 桌面天气时钟 / 环境监测终端** ｜ 嵌入式软件独立开发
> - 基于 FreeRTOS 设计 **5 任务 + 9 事件位**架构（UI/网络/传感器/光敏/按键），以"LCD 单写者、USART1 单占用、共享数据临界区整块替换"消除并发撕裂与串扰。
> - 打通 ESP32-C3 AT 联网链路：512B 环形缓冲 + RXNE 中断收数、超时重试、CWJAP/CWSTATE/CIPSNTPTIME/HTTPCLIENT 完整时序，手写 JSON 字段解析；区分"AT 查询失败"与"确实掉线"，避免误判触发无谓重连。
> - 低功耗从"降载"做到"模组省电"：夜间切 `AT+SLEEP=1` Modem-sleep（约 80mA → 20mA，Wi-Fi 连接保持），退出时先恢复全速再补更 WiFi/SNTP/天气；夜间时间源切 DS1302（burst 读 + CH 检测 + 写后回读校验 + 失败回退），断网仍正常走时。
> - 实现按键手势识别（单击/双击/三击/长按）并与光敏自动模式仲裁，收敛到单一状态机裁决者。
> - 将 839KB 界面资源迁移至 W25Q64 + littlefs，程序总 ROM 由 347KB 降至 54.9KB（**−84.2%**）。

### 7.3 投「校招 / 转行通用（重潜力与工程素养）」

> **STM32F407 桌面天气时钟** ｜ 嵌入式软件独立开发（约 7300 行自研 C 代码）
> - 从零完成需求拆解、方案选型、驱动编写、RTOS 集成到资源与功耗优化的全流程，并输出约 950 行项目文档。
> - 采用"分模块裸机验证 → RTOS 集成 → 资源与功耗优化 → 交互与功耗细化"四阶段推进，每阶段保持可编译、可烧录、可演示；用宏开关让运行/烧录/测试三种模式共存于一份代码。
> - 用可量化指标验证优化效果：程序总 ROM 降低 84.2%，字模查找由 O(n) 降为 O(1)，夜间模组电流约 80mA → 20mA。
> - 具备独立定位隐性故障的能力：曾解决"微秒计时回绕导致延时提前返回""退出低功耗后仅半数数据刷新""光敏中断永不触发""RTC 秒值冻结但日志正常"等 9 类问题。

---

## 8. 使用与调整建议（投递前检查清单）

1. **如实调整措辞**：上文"独立开发"按你实际的参与程度改写（独立 / 主导 / 负责 XX 模块）；第 2 节"自研范围"表格请确认与实际手写代码一致——面试被追问第三方库细节时，如实说明用的是现成组件即可，不必夸大。
2. **数据时效**：本文数字取自 2026-09-18 代码快照（映射表 `MDK/Output/STM32F407.map`：RO 56072 / RW 127152 / ROM 56184）。改动代码后请重新编译并核对 `Total ROM Size` 再更新简历数字。
3. **按岗位选择**：第 7 节已备好三套；投"驱动向"强调 7.1，投"物联网/应用向"强调 7.2，投校招/转行用 7.3。
4. **可补充的现场演示点**：上电开机流程 → 主界面刷新 → 遮挡光敏切 OLED → **按键手动切回白天** → 拔网线观察重连日志 → 串口输出 `Program Size` 前后对比。
5. **安全提醒**：`User/Src/App.c` 顶部目前仍是**明文 WiFi SSID/密码与天气 API Key**（`ssid` / `password` / `weather_url`）。对外投递或公开仓库前请改成占位符（README 第 11 节已给出脱敏写法），避免凭据泄露。
6. **文档同步建议**：`Documents/README.md`（约 950 行）尚未收录按键任务（其注释引用的"8.7 节"目前不存在）、`AT+SLEEP` 省电位与顶层 `Resource/` 新目录结构；若要把 README 一并附上作为写作能力证明，建议先补齐这几处，避免面试官对照代码发现文档滞后。
7. **需要 Word/PDF 时**：第 1 节粘进简历正文，第 2–6 节导出为项目说明附件；第 0 节可直接投在线表单。

---

*本文档由项目源码与 `Documents/README.md` 交叉核对生成（代码快照 2026-09-18）；若代码后续演进，请以 `User/`、`BSP/`、`Resource/` 源码与最新构建日志为准。*
