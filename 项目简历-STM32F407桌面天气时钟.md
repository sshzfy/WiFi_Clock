# 项目简历 · STM32F407 桌面天气时钟

> 面向求职的嵌入式软件项目经历材料。
> 第 1 节可直接粘贴进简历正文；第 2–7 节用于项目介绍、面试讲述与技能关键词。
> 所有数据均取自本项目源码与 `Documents/README.md`（仓库快照 2026-09-15），可直接对应到代码。

---

## 1. 简历正文版（可直接粘贴，约 400 字）

**STM32F407 桌面天气时钟 / 室内环境监测终端** ｜ 嵌入式软件独立开发 ｜ STM32F407VET6 + FreeRTOS

- 独立完成一款桌面天气时钟终端：ESP32-C3 AT 模组联网，SNTP 校时 + 心知天气 API 获取实况天气，ST7789 彩屏（240×320）显示时钟/日期/天气/室内温湿度，SSD1306 OLED 负责夜间显示，光敏电阻自动切换昼夜模式，夜间进入低功耗状态。
- 基于 FreeRTOS 设计 4 任务架构（UI / 网络 / 传感器 / 光敏），以事件组替代轮询，贯彻"LCD 单写者""USART1 单占用""共享数据临界区整块替换"三条并发约定，消除显示撕裂与 AT 响应串扰。
- 将 839KB 字库与图片从 MCU 内部 Flash 迁移到 8MB W25Q64 + littlefs：MCU 内不再保留任何点阵/像素数据，**程序总 ROM 由 347KB 降至 53.5KB（−84.6%）**；自研字模 O(1) 区位码定位替代原有线性查表（平均 1878 次字符串比较），并用 DMA 乒乓双缓冲把 Flash 读取时间完全隐藏在屏幕发送之后。
- 设计"夜间低功耗 + 双屏切换"方案：网络与传感器周期任务冻结、退出时按独立事件位立即补更，配合光敏中断 2s 去抖与 ACK 握手，避免竞态；夜间时间源切换到 DS1302 外部 RTC（写后回读校验 + CH 停振位检测 + 失败回退软件时钟），断网仍可正常走时。
- 交付三种构建模式（RTOS 运行 / 资源烧录 / 裸机模块测试）与 W25Q64、littlefs 共 20 项自测用例，资源缺失时渲染安全降级为纯色，不阻塞任务、不重启、不自动格式化。

**技术关键词**：STM32F407 · Cortex-M4 · FreeRTOS · 事件组/任务通知 · SPI + DMA · W25Q64 · littlefs · ST7789 · SSD1306 · 单总线 DHT22 · DS1302 · AT 指令 · SNTP · JSON 解析 · EXTI/ADC · Keil MDK · ST-Link

---

## 2. 项目基本信息

| 项目 | 内容 |
| --- | --- |
| 项目名称 | STM32F407 桌面天气时钟 / 室内环境监测终端 |
| 项目角色 | 嵌入式软件独立开发（驱动 → 应用 → 资源存储方案全流程） |
| 主控平台 | STM32F407VET6（Cortex-M4F，168MHz，512KB Flash，128KB SRAM + 64KB CCM） |
| 软件平台 | FreeRTOS（heap_4，88KB 堆，tick 1000Hz）+ littlefs v2.11 + STM32F4 标准外设库 |
| 开发环境 | Keil MDK-ARM Plus 5.24.1 / ARMCC V5.06（AC5）/ C99 / ST-Link |
| 代码规模 | 自研 C 代码约 6600 行：24 个 BSP 驱动文件 + 16 个应用层文件 + 界面绘制逻辑；工程共 9 个分组 99 个文件 |
| 成果指标 | 程序总 ROM 53.5KB（Flash 占用 10.4%）、SRAM 124.2KB、839KB 资源外置存储并由 littlefs 管理 |

### 硬件构成

| 模块 | 型号 / 参数 | 作用 |
| --- | --- | --- |
| 彩屏 | ST7789V，SPI3 @21MHz，240×320，RGB565，DMA1_Stream5 | 主界面显示 |
| 夜间屏 | SSD1306 0.96"，128×64，软件 I2C（PB6/PB7） | 夜间时间显示 |
| 联网模组 | ESP32-C3-MINI-1，AT 固件 v4.1.1.0，USART1 @115200 | WiFi / SNTP / HTTP |
| 温湿度 | DHT22 / AM2302 单总线（PE6） | 室内环境采集 |
| 光照 | 光敏电阻模块，DO(EXTI1) 或 AO(ADC+AWD) 二选一 | 昼夜自动切换 |
| 外部 Flash | W25Q64，8MB，SPI1 @21MHz | littlefs 存放全部字库与图片 |
| 外部 RTC | DS1302（PE7/PE8/PE9） | 夜间低功耗时间源 |
| 调试口 | USART2 @115200，DMA 发送 | 日志与调试 |

---

## 3. 详细版项目经历（面试 / 项目介绍用）

### 3.1 项目背景与目标

做一个放在桌面上长期无人值守运行的天气时钟：白天用彩屏展示时钟、日期星期、WiFi 状态、实况天气与室内温湿度；夜间自动切到低亮度 OLED 只显示时间，并降低系统负载。核心难点不在单个外设能否点亮，而在于**多任务并发下的显示一致性、断网/掉线时的自恢复、以及资源受限下 839KB 字库图片的存放**。

### 3.2 系统架构

```
应用层     main.c / Board.c / App.c / app_task.c / Provision.c / bare_test.c / Page/*.c
板级驱动   Asset / LCD / OLED / AT / I2C / DHT22 / External_RTC / Light_Sensor / Timer / Usart / W25Q64 / Profiling
操作系统   FreeRTOS（heap_4 / 事件组 / 任务通知 / 临界区）
文件系统   littlefs v2.11（lfs_port.c 对接 W25Q64，LFS_Operation 封装读写）
硬件抽象   STM32F4xx 标准外设库 + CMSIS
```

**4 个任务与优先级设计**

| 任务 | 优先级 | 栈 | 职责 | 周期 |
| --- | --- | --- | --- | --- |
| `UI_Task` | 3 | 4KB | 事件组创建、板级初始化、开机页→结果页→主界面；LCD 唯一写者；昼夜切换执行者 | 白天 500ms / 夜间 2s |
| `DHT22_Task` | 4 | 2KB | 单总线温湿度采集 | 10s |
| `Net_Task` | 2 | 4KB | 独占 USART1/AT：联网、SNTP、天气，1s 心跳推进三个周期计数器 | 1s 心跳 |
| `LightSensor_Task` | 2 | 2KB | 光敏中断处理、去抖、发起昼夜切换 | 事件驱动 + 1s 兜底 |

优先级不是随手定的：**DHT22 优先级高于 UI**，是因为单总线协议对 us 级时序敏感，被 UI 抢占会直接读到校验和错误；**net 低于 ui**，避免一次长 AT 事务（最长 10s）把显示饿死。

**并发约定（三条硬规则）**

1. **LCD 只由 UI_Task 写**：net/sensor/light 任务只更新数据并置事件位，杜绝多任务同时操作 SPI3。
2. **USART1 只由 Net_Task 使用**：AT 响应是"请求—等待"模式，第二个使用者必然造成串扰。
3. **共享数据整块替换**：`wifi_info` / `weather_info` / `room_info` / 软件时钟的 `epoch + sync_ms` 全部在 `taskENTER_CRITICAL()` 内整体赋值，UI 永远读不到半写状态（64 位字段尤其必要）。

**事件驱动的数据流**：9 个事件位区分为"数据就绪"（天气/房间/WiFi 变化）与"动作请求"（进夜间/回白天/立即补更），UI 用一次 `xEventGroupWaitBits` 同时等待多类事件，避免任何任务轮询对方状态。

### 3.3 核心工作与技术难点

#### 难点一：839KB 字库与图片放不进 512KB Flash —— 迁移到 W25Q64 + littlefs，ROM 降低 84.6%

- **问题**：11 张字模表（GB2312 一级汉字 3755 字 × 16/22 点阵 + 9 张 ASCII 表）与 32 张图片合计约 839KB，编译进固件时 RO-data 达 323604 字节，总 ROM 347KB，Flash 余量紧张且无法再扩展界面。
- **方案**：在 W25Q64 上跑 littlefs（覆盖 8MB / 2048 个 4KB 块），把全部点阵与像素数据以文件形式存放，MCU 内只保留 `Font_t` / `Image_t` 描述表；新增 `Asset` 资源访问层统一负责挂载、自检与读取。
- **优化点**：
  - **字模 O(1) 定位**：按 GB2312 区位码直接算偏移 `((区码-0xB0)×94 + (位码-0xA1)) × 每字字节数`，替代旧实现的线性查表（每个汉字平均要 `strcmp` 1878 次）。
  - **常开文件句柄 + 位置缓存**：11 个字库文件句柄常驻，记录当前偏移，位置相同则跳过 `lfs_file_seek`，避免 CTZ skip-list 反复遍历。
  - **图片乒乓双缓冲**：分配 2×7680 字节缓冲，一块交给 DMA1_Stream5 发送时 CPU 同时从 SPI1 读下一块。**SPI1 与 SPI3 同为 21MHz** 是这一优化成立的前提——读 Flash 的时间被完全隐藏在屏幕发送之后；分配失败自动退化为单缓冲，不影响功能。
- **结果**：RO-data 323604 → **3748 字节**，总 ROM 355556 → **54792 字节（−84.6%）**，Flash 占用降到 53.5KB/512KB，为后续功能预留了充足空间。
- **工程化取舍**：资源总量超过 512KB Flash，单次编译装不下，因此设计了**三批编译烧录方案**（批次 1 汉字库 / 批次 2 全屏图 / 批次 3 ASCII 表 + 图标），用 `RESOURCE_PROVISION` + `PROVISION_BATCH` 两个宏控制，并在 `main()` 里提供独立的烧录模式（不启动 RTOS、不初始化 LCD，只开 W25Q64 与串口）。

#### 难点二：夜间低功耗与双屏切换的竞态控制

- **方案**：光敏任务检测到亮度跳变后先做 2s 去抖（期间新中断重新计时），再置事件位请求切换，并**等待 UI 任务回 ACK（超时 2s）**；UI 任务进入夜间时先"用网络时间回写 DS1302 并回读校验"→ 置低功耗标志 → 开 OLED 显示时间 → 关 ST7789 显示与背光。
- **关键细节**：夜间 `Net_Task` / `DHT22_Task` **冻结周期计数器且不执行任何 update**（`if (s_lowpower) continue;` 必须放在 `--counter` 之前，否则退出夜间时三个计数器同时归零、同一秒内连发多次 AT 事务）；退出夜间时 UI 通过 `EV_NET_UPDATE_NOW` / `EV_SENSOR_UPDATE_NOW` 两个**独立事件位**通知两个任务立即补更。
- **踩坑记录（事件组清除式等待）**：最初两个任务共用同一个事件位，先被唤醒的任务会把位清掉，另一个永远收不到通知，表现为"退出夜间后只有一半数据被刷新"；拆成两个位后解决。
- **夜间时间源切换**：`DS1302_SetTime()` 恒返回成功（无写校验），因此改为**写后回读比对"当日分钟总数"**，既容忍写耗时造成的 2 分钟偏差又能跨小时；同时检测秒寄存器 **CH（Clock Halt）位**，CH=1 说明振荡器停走，直接判定失败并回退到软件时钟——否则界面会显示一个不动的钟而日志仍报 `time_source=RTC`，属于典型的"看起来正常但时间是错的"故障。

#### 难点三：联网链路的健壮性

- **AT 指令层**：USART1 用 **RXNE 中断 + 512 字节环形缓冲**收数，AT 层按毫秒级超时轮询取出，等待期间 `vTaskDelay(1)` 让出 CPU；封装 `AT_Init / AT+RESET / 恢复出厂 / CWJAP / CWSTATE / CWJAP? / CIPSNTPCFG / CIPSNTPTIME / HTTPCLIENT` 完整时序，并区分 OK / ERROR / BUSY / READY 四种应答。
- **天气 JSON 解析**：不引入 JSON 库，直接在响应体中按字段定位 `results → location → name/path` 与 `now → text/code/temperature`，26 条天气码映射到 22 张图标，未知码回落为晴天。
- **层级自恢复**：WiFi 掉线自动重连，重连成功后立即补一次 SNTP 与天气；SNTP 失败按 5s、天气失败按 60s 重试，成功后分别回到 4h / 1h 周期；`AT_SNTP_Get_Time` 会把 1970 年之类的无效时间判为失败继续轮询。
- **软件时钟**：SNTP 校时后用 TIM5 1ms 计数推算当前时间，日期换算采用**民用历算法**（Howard Hinnant），未同步时界面显示 `--:--` 而不是错误时间。

#### 难点四：驱动层时序与"隐性状态"问题排查

| 现象 | 根因 | 处理 |
| --- | --- | --- |
| 上电先闪出上次的旧画面 | ST7789 复位**不清 GRAM** | `ST7789_Init()` 在开显示与背光**之前**先刷黑 GRAM |
| 切回 RTOS 固件后 OLED 一直挂着裸机测试画面 | SSD1306 不断电就保持 GDDRAM 与显示状态 | 上电即 `OLED_Init()` + `OLED_Display_Off()`；`OLED_Init()` 内**先清屏、最后才 `0xAF` 开显示** |
| OLED 清屏耗时约 240ms | 逐列发送需要 1024 次单字节 I2C 事务 | 改为**按页整块写**，每页 1 次事务写 128 字节，共 8 次 |
| 遮挡光敏后毫无反应、串口也无日志 | 改了 GPIO 端口/引脚，但 EXTI 的 `PortSource`/`PinSource` 仍是旧端口，中断永不触发 | 两者必须成对修改（本项目实测踩过一次 PC1 vs PA1） |
| 周一时间同步失败 | 星期语义混用：DS1302 寄存器为 1~7，旧代码写 `weekday-1` 得到 0 | 统一为 1~7，读回时 `week < 1` 直接判失败 |
| W25Q64 未焊接时程序卡死 | 忙等待无上限 | 加入 `W25Q64_BUSY_TIMEOUT` 超时计数与 `W25Q64_GetBusyTimeoutCount()` 统计 |

- **中断与 RTOS 约束**：NVIC 分组为 `PriorityGroup_4`；`configMAX_SYSCALL_INTERRUPT_PRIORITY = 5<<4`，因此 **TIM5（优先级 2，1ms 基准）中断里只做计数，禁止调用任何 FreeRTOS API**；USART1 / EXTI1 / ADC（优先级 5）才允许调用 `...FromISR` 并在退出时 `portYIELD_FROM_ISR`。
- **调试日志**：`printf` 重定向到 `fputc`，按行加锁（互斥量 + 行属主），遇 `\n` 或缓冲满时以 DMA 整行发出，保证多任务日志不交叉；调度器未运行或中断上下文中退化为逐字节轮询，使启动期与 HardFault 现场的日志也能打印（`vAssertCalled`、`vApplicationStackOverflowHook` 都依赖它）。
- **性能测量**：用 `DWT->CYCCNT` 实现微秒级计时（`Prof_Us()`），不占用定时器、不产生中断，用于评估渲染耗时。

### 3.4 工程化与可维护性

- **三种构建模式由宏切换**，一份代码同时承载运行、生产与测试：
  | 宏组合 | 模式 | 行为 |
  | --- | --- | --- |
  | `RESOURCE_PROVISION=1` | 资源烧录 | 只初始化 W25Q64 与串口，不启动 RTOS、不初始化 LCD |
  | `USE_FREERTOS=1` | RTOS 运行 | 正式固件 |
  | `USE_FREERTOS=0` | 裸机模块测试 | 用 `BM_TEST_MODULE` 选择 OLED / 光敏 / DS1302 / W25Q64 / littlefs |
- **自测用例覆盖底层风险**：W25Q64 用例 T1~T9（JEDEC ID、页编程、跨页写入、扇区擦除、忙等待超时统计，测试区固定在末 4 个扇区不碰文件系统）；littlefs 用例 T1~T11（读写、目录、追加、64KB 大文件、遍历、删除、卸载重挂载持久化、`SaveFont`、`SaveImage`+`LoadImage`、缺失文件返回 `NOENT`）。
- **失败安全降级**：`Asset_Init()` 开机逐项核对文件是否存在、大小是否正确，统计"已检查项 / 缺失项"；资源缺失时渲染退化为纯色背景并在串口告警，**绝不自动格式化文件系统**，不阻塞任务、不重启。
- **分层与规范**：BSP 驱动与业务逻辑分离（24 个 BSP 文件全部 UTF-8），引脚/周期/阈值/开关全部宏定义集中管理，避免散落魔数；界面中文按 GB2312 双字节写入并配套区位码定位，保证字模不错位。
- 附带完整项目文档 `Documents/README.md`（约 950 行）：目录结构、任务与事件位表、引脚分配、中断优先级约束、关键流程时序、资源布局与烧录步骤、已知限制清单（20 项）。

---

## 4. 量化成果一览

| 指标 | 数值 | 说明 |
| --- | --- | --- |
| 程序总 ROM | **355556 → 54792 字节（−84.6%）** | 资源外置后的 Flash 占用 |
| RO-data | **323604 → 3748 字节** | MCU 内只剩描述表与指针表 |
| Flash 占用 | 53.5KB / 512KB（约 10.4%） | 余量充足 |
| SRAM 占用 | 124.2KB / 128KB（FreeRTOS 堆 88KB + 渲染缓冲 23KB + 任务栈 13KB） | 静态余量约 3.9KB，堆内余量约 60KB |
| 外置资源规模 | 858778 字节（≈839KB），littlefs 实际占用约 247/2048 块 | 字库 + 图片 |
| 字模定位复杂度 | 线性查表（平均 1878 次 `strcmp`）→ **O(1)** | 区位码直接算偏移 |
| 图片传输 | 15KB 乒乓双缓冲，Flash 读取时间完全隐藏在 DMA 发送之后 | 全屏图约 58ms，受 SPI3@21MHz 发送带宽限制 |
| 并发规模 | 4 任务 / 5 优先级 / 9 个事件位 / 1 个互斥量 | 事件驱动，无轮询等待 |
| 底层自测 | W25Q64 9 项 + littlefs 11 项 | 裸机模式可独立验证 |
| 自研代码 | 约 6600 行（BSP 3435 + 应用层 2212 + 界面 518 + 文件系统适配 475） | 不含原厂库与资源数据 |

---

## 5. 技能映射（对照岗位 JD 使用）

| 岗位要求常见表述 | 本项目可对应的证据 |
| --- | --- |
| 熟悉 ARM Cortex-M 平台开发 | STM32F407（Cortex-M4F）168MHz，PLL 时钟树配置、NVIC 优先级分组、DMA、EXTI、ADC、SPI、USART、TIM 全流程使用 |
| 熟悉 RTOS 原理与应用 | FreeRTOS 4 任务架构、优先级设计权衡、事件组、任务通知（`vTaskNotifyGiveFromISR`）、临界区、互斥量、heap_4、栈溢出钩子与断言钩子 |
| 熟悉常用外设驱动开发 | 独立编写 SPI Flash（W25Q64 全部指令/页编程/擦除/超时）、ST7789、SSD1306、DHT22 单总线、DS1302 三线 RTC、软件 I2C、光敏 ADC/EXTI |
| 熟悉通信协议与联网 | ESP32-C3 AT 指令集、WiFi 连接状态解析、SNTP 网络校时、HTTP 客户端、JSON 字段解析、断线重连与重试策略 |
| 熟悉文件系统 / 存储 | littlefs 移植（read/prog/erase 回调对接 SPI Flash）、挂载/自检/大文件读写/掉电持久化验证 |
| 具备调试与问题定位能力 | 通过串口日志定位"事件位被抢占""EXTI 端口源不匹配""DS1302 CH 停振""复位不清 GRAM"等 6 类隐性故障 |
| 代码规范与文档能力 | 分层架构、宏集中配置、失败安全降级、约 950 行项目文档与三模式构建说明 |
| 工具链 | Keil MDK-ARM、ARMCC V5.06、ST-Link 调试、DWT 性能剖析、Git 版本控制（含 `.gitignore` 规范） |

---

## 6. 面试可展开的三个技术故事（建议按 STAR 讲）

1. **"把 839KB 资源搬出 MCU"**：从"Flash 装不下"的问题出发 → 调研 littlefs + 外部 Flash → 自研资源访问层 → 用区位码把字模查找做成 O(1) → 用乒乓双缓冲把 Flash 读取藏进 DMA 发送 → 量化出 ROM 降低 84.6%。可延伸讨论：为什么不在 Flash 里做 XIP（外部 SPI 无地址映射）、littlefs 掉电安全与磨损均衡、为什么保留源码内资源作为烧录备份。
2. **"夜间只有一半数据被刷新"**：症状 → 排查到事件组清除式等待被两个任务共用 → 拆成独立事件位 → 补充"冻结计数器放递减之前"的第二个坑。体现对 RTOS 原语语义的理解而非停留在 API 调用。
3. **"界面显示一个不动的钟，日志却报时间源正常"**：DS1302 CH 位导致秒值冻结 → 增加 CH 检测与回退逻辑 → 顺带发现星期 1~7 语义混用导致周一同期失败 → 用"当日分钟总数比较"替代逐字段比较以容忍写耗时。体现对数据手册细节的敏感度。

---

## 7. 使用与调整建议

1. **投递前请按真实情况调整措辞**：上文"独立开发"按你实际的参与程度改写（独立 / 主导 / 负责 XX 模块）。
2. **按岗位裁剪**：投"MCU 驱动"岗强调第 3.3 节前两条与驱动清单；投"物联网/应用"岗强调 AT 联网、自恢复与低功耗；投"应届生"岗保留量化数据与踩坑故事即可。
3. **可补充的现场演示点**：上电开机流程 → 主界面刷新 → 遮挡光敏切 OLED → 拔网线观察重连日志 → 串口输出 `Program Size` 对比。
4. **安全提醒**：`User/Src/App.c` 顶部目前仍是**明文 WiFi SSID/密码与天气 API Key**。对外投递或公开仓库前，请改成占位符（README 第 11 节已给出脱敏写法），避免凭据泄露。
5. 需要 Word/PDF 版本时，可把第 1 节直接粘进简历，第 2–5 节导出为项目说明附件。

---

*本文档由项目源码与 `Documents/README.md`（仓库快照 2026-09-15）交叉核对生成；若代码后续演进，请以 `User/`、`BSP/` 源码与最新构建日志为准。*
